services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: rd
      POSTGRES_PASSWORD: rd
      POSTGRES_DB: lease_again
    ports:
      - "5432:5432"
    volumes:
      - lease_again_pg:/var/lib/postgresql/data

volumes:
  lease_again_pg:
